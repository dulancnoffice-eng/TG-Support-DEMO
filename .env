PORT=3000
NODE_ENV=development
APP_ORIGIN=http://localhost:3000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tg_support
JWT_SECRET=replace-with-a-long-random-secret
# 64 hex characters = 32 bytes. Generate with: openssl rand -hex 32
BOT_TOKEN_ENCRYPTION_KEY=replace-with-64-hex-characters

# Bootstrap only. On first startup the server auto-creates the DB schema, then creates this Master Admin.
MASTER_ADMIN_ID=DLXN17
MASTER_ADMIN_NAME=DULAN MENAKA 
MASTER_ADMIN_EMAIL=dulan.cnoffice@gmail.com
MASTER_ADMIN_PASSWORD=Dulan@12345TG
